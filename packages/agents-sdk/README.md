# @omniagents/agents-sdk

Lightweight client SDK utilities.

## Exports
- submitLead(apiBase, payload)

## Example
```ts
import { submitLead } from '@agents-sdk';
await submitLead('http://localhost:8080', { name: 'A', email: 'a@b.com' });
```
